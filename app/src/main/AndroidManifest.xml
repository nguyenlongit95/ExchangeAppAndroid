<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exchangeapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GiaVangTheGioiActivity"></activity>
        <activity android:name=".XangDauActivity" />
        <activity android:name=".TienAoDetailActivity" />
        <activity android:name=".TienAoActivity" />
        <activity android:name=".LaiSuatActivity" />
        <activity android:name=".GiaVangActivity" />
        <activity android:name=".NgoaiTeActivity" />
        <activity android:name=".TyGiaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>